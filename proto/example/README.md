# exemplo-login

Projeto de exemplo para o protótipo da TL-ADE. Um formulário de login em JavaScript puro, testado com Vitest + happy-dom.

Comandos: `npm test`.

Regra do projeto: toda correção de comportamento vem com um teste em `src/*.test.js` que falha antes e passa depois.
